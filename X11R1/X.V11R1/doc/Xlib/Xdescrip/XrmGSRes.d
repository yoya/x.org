The
.PN XrmGetSearchResource
function searches the specified database levels for the resource that is
fully identified by the specified name and class.
If it finds the resource,
.PN XrmGetSearchResource
first converts it to the specified type and then returns this converted
resource to the value argument.
